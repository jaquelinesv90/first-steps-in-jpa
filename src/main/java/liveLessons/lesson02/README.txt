This lesson's focus is working in JPQL queries.

JPQL is a query language that allows you to define database queries based on your entity model.
It uses the entity object model instead of database table to define a query.

